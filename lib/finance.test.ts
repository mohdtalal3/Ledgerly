import{describe,expect,it}from"vitest";import{calculateLoanOutstanding,calculateTax,calculateTransferFee,calculateTransferNetAmount,convertMoney,signedAmount,sum}from"./finance";
describe("financial calculations",()=>{it("converts USD to PKR without binary float drift",()=>{expect(convertMoney("100","USD","280")).toEqual({amountUsd:"100.0000",amountPkr:"28000.0000"})});it("converts PKR to USD",()=>{expect(convertMoney("28000","PKR","280").amountUsd).toBe("100.0000")});it("calculates configurable tax",()=>{expect(calculateTax("100000","5")).toBe("5000.0000")});it("calculates fixed and percentage transfer fees exactly",()=>{expect(calculateTransferFee("250.55","3.25","fixed")).toBe("3.2500");expect(calculateTransferFee("250.55","2.5","percent")).toBe("6.2638")});it("deducts a transfer fee from the original amount",()=>expect(calculateTransferNetAmount("400","12")).toBe("388.0000"));it("rejects a fee that consumes the transfer",()=>expect(()=>calculateTransferNetAmount("400","400")).toThrow());it("rejects transfer fee percentages above 100",()=>expect(()=>calculateTransferFee("100","100.01","percent")).toThrow());it("calculates a person's loan balance exactly",()=>expect(calculateLoanOutstanding([{entryType:"lend",amountPkr:"100.10"},{entryType:"lend",amountPkr:"50.20"},{entryType:"repayment",amountPkr:"25.05"}])).toBe("125.2500"));it("uses signed ledger directions",()=>{expect(signedAmount("income","15").toString()).toBe("15");expect(signedAmount("tax_payment","15").toString()).toBe("-15")});it("sums decimal values exactly",()=>expect(sum(["0.1","0.2"])).toBe("0.3000"))});
