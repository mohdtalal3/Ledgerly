import Link from"next/link";import{Download,Plus}from"lucide-react";import{TransactionList}from"@/components/transaction-list";import{getTransactions}from"@/lib/data";
export const metadata={title:"Transactions"};export default async function Transactions({searchParams}:{searchParams:Promise<{month?:string;search?:string;type?:string}>}){const q=await searchParams;const month=q.month??new Date().toISOString().slice(0,7);const items=await getTransactions({month,search:q.search,type:q.type});return <><header className="page-header"><div><div className="eyebrow">Complete ledger</div><h1>Transactions</h1><p className="muted">Every movement, in one searchable timeline.</p></div><Link className="btn primary icon" href="/transactions/new"><Plus/></Link></header><div className="toolbar"><form><input name="search" placeholder="Search…" defaultValue={q.search}/><input name="month" type="month" defaultValue={month}/><select name="type" defaultValue={q.type??""}><option value="">All types</option><option value="income">Income</option><option value="expense">Expense</option><option value="transfer_in">Transfers in</option><option value="transfer_out">Transfers out</option><option value="tax_payment">Tax payments</option></select><button className="btn">Apply</button></form><Link className="btn" href={`/api/export?month=${month}`}><Download size={16}/>CSV</Link></div><TransactionList transactions={items} editable/></>}
