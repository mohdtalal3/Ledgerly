import Link from"next/link";import{Download}from"lucide-react";import{IncomeExpenseChart}from"@/components/charts";import{Money}from"@/components/currency";import{getDashboard}from"@/lib/data";
export const metadata={title:"Reports"};export default async function Reports({searchParams}:{searchParams:Promise<{month?:string}>}){const q=await searchParams;const month=q.month??new Date().toISOString().slice(0,7);const d=await getDashboard(month);const expenses=d.transactions.filter(t=>t.type==="expense");return <><header className="page-header"><div><div className="eyebrow">Analytics & export</div><h1>Reports</h1><p className="muted">Patterns you can act on, with raw data when you need it.</p></div><Link className="btn" href={`/api/export?month=${month}`}><Download size={17}/>Export CSV</Link></header><div className="toolbar"><form><input name="month" type="month" defaultValue={month}/><button className="btn">View report</button></form></div><section className="grid summary-grid"><Card label="Income" value={d.income}/><Card label="Expenses" value={d.expenses}/><Card label="Savings" value={d.net}/><Card label="Daily spend" value={d.expenses/Math.max(1,new Date().getDate())}/></section><section className="section card"><div className="section-title"><h2>Income vs expenses</h2><span className="chip">{month}</span></div><IncomeExpenseChart income={d.income} expenses={d.expenses}/></section><section className="section card"><div className="section-title"><h2>Detailed expenses</h2></div><div className="table-wrap"><table className="data-table"><thead><tr><th>Date</th><th>Description</th><th>Category</th><th>Account</th><th>Original</th><th>PKR value</th></tr></thead><tbody>{expenses.map(t=><tr key={t.id}><td>{t.transaction_date}</td><td>{t.merchant||t.description}</td><td>{t.expense_categories?.name}</td><td>{t.accounts?.name}</td><td><Money value={t.original_amount} currency={t.original_currency}/></td><td><Money value={t.amount_pkr}/></td></tr>)}</tbody></table></div></section></>}
function Card({label,value}:{label:string;value:number}){return <div className="card summary-card"><span className="muted small">{label}</span><Money value={value} className="medium"/></div>}
