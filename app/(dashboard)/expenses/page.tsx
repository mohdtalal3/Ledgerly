import Link from"next/link";import{Plus}from"lucide-react";import{Money}from"@/components/currency";import{TransactionList}from"@/components/transaction-list";import{getTransactions}from"@/lib/data";
export const metadata={title:"Expenses"};export default async function Expenses({searchParams}:{searchParams:Promise<{month?:string;search?:string}>}){const q=await searchParams;const month=q.month??new Date().toISOString().slice(0,7);const items=await getTransactions({type:"expense",month,search:q.search});const total=items.reduce((n,t)=>n+Number(t.amount_pkr),0);return <><header className="page-header"><div><div className="eyebrow">Money out</div><h1>Expenses</h1><p className="muted">See where your money goes without the noise.</p></div><Link className="btn primary icon" href="/transactions/new"><Plus/></Link></header><div className="grid summary-grid"><div className="card summary-card wide"><span className="muted">Spent this period</span><Money value={total} className="large"/><span className="small">{items.length} payments</span></div><div className="card summary-card"><span className="muted small">Daily average</span><Money value={total/new Date().getDate()} className="medium"/></div><div className="card summary-card"><span className="muted small">Largest expense</span><Money value={Math.max(0,...items.map(t=>Number(t.amount_pkr)))} className="medium"/></div></div><div className="section toolbar"><form><input aria-label="Search expenses" name="search" placeholder="Search expenses…" defaultValue={q.search}/><input aria-label="Month" name="month" type="month" defaultValue={month}/><button className="btn">Filter</button></form></div><TransactionList transactions={items} editable/></>}
