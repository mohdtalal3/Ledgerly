"use client";
import {useState} from "react";import {useRouter} from "next/navigation";import {LoaderCircle,LockKeyhole} from "lucide-react";
export function LoginForm(){const router=useRouter();const[pin,setPin]=useState("");const[error,setError]=useState("");const[loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError("");const res=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({pin})});const data=await res.json();if(!res.ok){setError(data.error??"Unable to sign in");setLoading(false);return}router.replace("/dashboard");router.refresh()}return <form className="form-grid" style={{marginTop:28}} onSubmit={submit}><div className="field"><label htmlFor="pin">PRIVATE PIN</label><input id="pin" className="pin-input" type="password" inputMode="numeric" autoComplete="current-password" maxLength={64} value={pin} onChange={e=>setPin(e.target.value)} autoFocus required/></div>{error&&<div className="form-message">{error}</div>}<button className="btn primary" disabled={loading}>{loading?<LoaderCircle className="animate-spin" size={19}/>:<LockKeyhole size={18}/>}Unlock workspace</button></form>}
